general:
  branches:
    only:
      - ci-test

machine:
  timezone:
    America/Los_Angeles
  ruby:
    version:
      2.2.5
  node:
    version: 
      0.12.2

dependencies:
  post:
    - gem install jekyll
    - cd $HOME/$CIRCLE_PROJECT_REPONAME/_script && npm install && node process.js

test:
  override:
    - echo "No tests are needed"

deployment:
  production:
    branch: ci-test
    commands:
      - jekyll build
